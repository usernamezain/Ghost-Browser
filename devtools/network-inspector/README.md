# network-inspector/

## Purpose
A built-in network request inspector that hooks into Gecko's request interception layer, providing functionality similar to browser DevTools' Network tab but with additional features: header/request editing for manual testing, and local HTTP archive (HAR) export for research documentation.

## Roadmap Phase
Populated in **Phase 4** (Developer & Researcher Tools). Currently empty.
